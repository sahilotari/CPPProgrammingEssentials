Everytime a function is called, it gets created in stack memory.
All the local variables belonging to that function call are part of the stack frame
Once the function is finished, the stack frame is cleared off.
The function that is currently running is always on the top of the stack